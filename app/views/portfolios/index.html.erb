<p id="notice"><%= notice %></p>

<h2>My project's portfolio</h2>

<hr>

<div class="row">
  <% @portfolios.each do |portfolio| %>
    <div class="col-md-4 portfolio-item">
      <h3>
        <%= link_to portfolio.title, portfolio %>
      </h3>
      <p>
        <%= portfolio.description %>
      </p>
    </div>
  <% end %>
</div>

<hr>